/* ============== ГЛОБАЛЬНЫЙ NAMESPACE ==============
   Здесь живут все объекты, которые читаются и мутируются из разных модулей.
   Использование: import { G } from './globals.js'; G.state.day++;
   Реассайн через G.state = ... (а не let state = ..., чтобы изменения видели все модули). */

export const G = {
  /* состояние игры (мутируется по ходу симуляции, реассайнится при freshState/restore) */
  state: null,

  /* режим: 'menu' (главное меню с панорамой) | 'playing' (обычный геймплей) */
  mode: 'menu',
  menuTime: 0,           // время в меню для авто-панорамирования камеры

  /* камера */
  cam: { x: 64, y: 64, z: 0.225 },

  /* выбор и UI */
  sel: null,            // {type:'b'|'c'|'hero', id}
  buildSel: null,       // id здания | 'demolish'
  floaters: [],         // всплывающие тексты "+5 🪙"
  covDirty: true,
  frameNo: 0,

  /* индексы и кэши */
  BIDX: new Map(),      // building id -> object

  /* спрайт-кэши (заполняются в sprites.js при инициализации) */
  BSPR: {},             // здания по ID
  HSPR: [],             // дома по уровню
  TSPR: {},             // тайлы земли по типу
  TREES: [],
  ROCKS: [],
  CIT_M: [],            // спрайты горожан мужчин
  CIT_F: [],
  USPR: {},             // спрайты войск
  FSPR: {},             // спрайты врагов
  HERO_S: null,         // спрайт героя
  PPL_F: {},            // зеркальные спрайты

  /* буфер ввода */
  pinch: null,
};
